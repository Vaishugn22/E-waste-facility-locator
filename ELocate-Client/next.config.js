/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // Optional: You can add this for best practices
  images: {
    domains: ['avatars.githubusercontent.com'], 
  },
};

module.exports = nextConfig;
